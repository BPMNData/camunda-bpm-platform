<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="de.hpi.uni.potsdam.test.bpmn_to_sql.db">
  <select id="selectSingleTraveldetails" parameterType="java.util.Map" resultType="java.util.HashMap">
    select travelID, state, departure, destination, start_date, return_date, hotel, price from TravelDetails 
    <where>
        <if test="travelID != null">
            travelID = #{travelID}
        </if>
        <if test="state != null">
            and state = #{state}
        </if>
        <if test="departure != null">
            departure = #{departure}
        </if>
        <if test="destination != null">
            and destination = #{destination}
        </if>
        <if test="start_date != null">
            start_date = #{start_date}
        </if>
        <if test="return_date != null">
            and return_date = #{return_date}
        </if>
        <if test="hotel != null">
            hotel = #{hotel}
        </if>
        <if test="price != null">
            and price = #{price}
        </if>
    </where>
  </select>
  
  <select id="selectSingleFlights" parameterType="java.util.Map" resultType="java.util.HashMap">
    select flightID, travelID, airline, inboundFlightNumber, outboundFlightNumber, price, state from Flights 
    <where>
        <if test="flightID != null">
            flightID = #{flightID}
        </if>
        <if test="travelID != null">
            and travelID = #{travelID}
        </if>
        <if test="airline != null">
            airline = #{airline}
        </if>
        <if test="inboundFlightNumber != null">
            and inboundFlightNumber = #{inboundFlightNumber}
        </if>
        <if test="outboundFlightNumber != null">
            outboundFlightNumber = #{outboundFlightNumber}
        </if>
        <if test="price != null">
            and price = #{price}
        </if>
        <if test="state != null">
            state = #{state}
        </if>
    </where>
  </select>
</mapper>
